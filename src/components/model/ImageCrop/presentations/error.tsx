import type { FC } from 'react';

export const ImageCropErrorPresentation: FC = () => (
  <div className="w-full h-full items-center justify-center flex">
    <strong>画像を読み込むことができませんでした</strong>
  </div>
);
